# Travese mode, set global res, no return

 self.lastVal = root.val  #Mistake fogot set lastVal in mid-order approach dfs

 iterative approach exactly the same with the mid-order but set last after check 